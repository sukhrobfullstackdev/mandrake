'use client';

import DeviceVerificationStart from '@components/device-registration/device-verification-start';

const DeviceVerificationStartPage = () => {
  return <DeviceVerificationStart />;
};

export default DeviceVerificationStartPage;
